# Database Migrations

This folder will hold all alembic/flask-migrate generated migration scripts.
Since we use `flask db migrate` in the backend, any generated migrations will reflect changes to the SQLAlchemy models here.
